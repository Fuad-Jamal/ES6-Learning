class product {
    constructor(name, price){
        this.name = name
        this.price = price
    }
    getDetails(){
        console.log(`The product we have is ${this.name} which costs about ${this.price}`)
    }
    getDiscount(disc){
        git a
    }
}